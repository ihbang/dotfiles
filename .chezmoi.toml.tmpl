{{- $email := promptStringOnce . "email" "Email address for github account" -}}

[git]
autoCommit = true
autoPush = true

[data]
email = {{ $email | quote }}
editor = "nvim"
{{/* The personal wiki is not configurable: it always lives under ~/Documents, and
     run_once_after_10_clone-wiki.sh clones it there. Kept as a data value so the
     path is declared once; that script template is its only consumer. */ -}}
wikiPath = {{ printf "%s/Documents/llm-wiki" .chezmoi.homeDir | quote }}
